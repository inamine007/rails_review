<!DOCTYPE html>
<html>
  <head>
    <title>Rails Review</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header class="app-title navbar navbar-expand-md navbar-light bg-light">
      <div class="navbar-brand">
        Rails Review
      </div>
      <ul class="navbar-nav ml-auto">
        <% if current_user %>
          <li class="nav-item"><%= link_to 'マイページ', root_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to 'ログアウト', logout_path, method: :delete, class: "nav-link" %></li>
        <% else %>
          <li class="nav-item"><%= link_to '会員登録', users_path, class: "nav-link" %></li>
          <li class="nav-item"><%= link_to 'ログイン', login_path, class: "nav-link" %></li>
        <% end %>
      </ul>
    </header>
    <div class="container">
      <% if flash.notice.present? %>
        <div class="alert alert-success"><%= flash.notice %></div>
      <% end %>
      <% if flash[:danger].present? %>
        <ul class="alert alert-danger">
          <% flash[:danger].each do |d| %>
            <li><%= d %></li>
          <% end %>
        </ul>
      <% end %>
      <%= yield %>
    </div>
    <footer>
      <small>© 2022 Ruby on Rails ReviewSite</small>
    </footer>
  </body>
</html>
